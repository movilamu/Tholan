import { ArrowUpRight } from 'lucide-react';
import Link from 'next/link';
export function StatCard({label,value,icon:Icon,color,href,empty}:{label:string;value:string|number;icon:any;color:string;href:string;empty?:boolean}){return <Link href={href} className="app-surface rounded-2xl p-5 block group hover:translate-y-[-2px] transition-transform"><div className="flex items-start justify-between"><span className="h-10 w-10 rounded-xl flex items-center justify-center" style={{background:`color-mix(in srgb, ${color} 12%, var(--surface))`,color}}><Icon size={20}/></span><ArrowUpRight size={18} style={{color:'var(--muted)'}} className="opacity-70 group-hover:opacity-100"/></div><div className="mt-5 text-sm font-semibold" style={{color:'var(--muted)'}}>{label}</div><div className="mt-1 text-2xl font-black">{empty?<span className="text-base font-semibold">—</span>:value}</div></Link>}
